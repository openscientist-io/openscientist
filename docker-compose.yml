# Docker Compose for Open Scientist
#
# Usage:
#   Development: Set OPEN_SCIENTIST_DEV_MODE=true in .env for auto-reload & mock auth
#
#   Production:  docker compose up  (OPEN_SCIENTIST_DEV_MODE unset or false)

services:
  postgres:
    image: postgres:18
    restart: ${OPEN_SCIENTIST_RESTART_POLICY:-unless-stopped}
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-open_scientist}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-open_scientist_dev_password}
      POSTGRES_DB: ${POSTGRES_DB:-open_scientist}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql
      # Init script creates open_scientist_admin role for RLS bypass
      - ./docker/postgres/init.sql:/docker-entrypoint-initdb.d/init.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-open_scientist} -d ${POSTGRES_DB:-open_scientist}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  open_scientist:
    stdin_open: true # docker run -i
    tty: true        # docker run -t
    platform: linux/amd64
    build: .
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "${PORT:-8080}:8080"
    # In dev mode, restart=no; in production, restart=unless-stopped
    restart: ${OPEN_SCIENTIST_RESTART_POLICY:-unless-stopped}
    # All credentials and settings come from .env (ANTHROPIC_API_KEY, CLAUDE_PROVIDER, etc.)
    env_file:
      - path: .env
        required: false
    environment:
      # Database URLs use the 'postgres' service hostname — must override any .env value
      DATABASE_URL: postgresql+asyncpg://${POSTGRES_USER:-open_scientist}:${POSTGRES_PASSWORD:-open_scientist_dev_password}@postgres:5432/${POSTGRES_DB:-open_scientist}
      ADMIN_DATABASE_URL: postgresql+asyncpg://open_scientist_admin:${POSTGRES_PASSWORD:-open_scientist_dev_password}@postgres:5432/${POSTGRES_DB:-open_scientist}
      PORT: ${PORT:-8080}

      # Phenix: host path (from .env) is mounted at /opt/phenix inside the container
      PHENIX_PATH: ${PHENIX_PATH:+/opt/phenix}

      # GCP credentials: the host file path is passed separately so agent containers
      # can mount it on the host side (GOOGLE_APPLICATION_CREDENTIALS is set in Dockerfile)
      GCP_CREDENTIALS_HOST_PATH: ${GCP_CREDENTIALS_FILE:-}

      # Container isolation defaults
      OPEN_SCIENTIST_USE_CONTAINER_ISOLATION: ${OPEN_SCIENTIST_USE_CONTAINER_ISOLATION:-true}
      OPEN_SCIENTIST_EXECUTOR_IMAGE: ${OPEN_SCIENTIST_EXECUTOR_IMAGE:-open_scientist-executor:latest}
      OPEN_SCIENTIST_EXECUTOR_MEMORY: ${OPEN_SCIENTIST_EXECUTOR_MEMORY:-2g}
      OPEN_SCIENTIST_EXECUTOR_CPU: ${OPEN_SCIENTIST_EXECUTOR_CPU:-0.5}
      OPEN_SCIENTIST_AGENT_MEMORY: ${OPEN_SCIENTIST_AGENT_MEMORY:-8g}
      OPEN_SCIENTIST_AGENT_CPU: ${OPEN_SCIENTIST_AGENT_CPU:-2.0}
      OPEN_SCIENTIST_HOST_PROJECT_DIR: ${OPEN_SCIENTIST_HOST_PROJECT_DIR:-}

      # Derived settings not expressible in .env
      PYTHONDONTWRITEBYTECODE: ${OPEN_SCIENTIST_DEV_MODE:+1}

    volumes:
      - ./jobs:/app/jobs
      - ./data:/app/data
      - ./src:/app/src  # Mount source for live reload in dev mode

      # Mount GCP credentials if using Vertex AI (set GCP_CREDENTIALS_FILE in .env)
      - ${GCP_CREDENTIALS_FILE:-/dev/null}:/app/gcp-credentials.json:ro

      # Mount Phenix installation if available (set PHENIX_PATH in .env)
      - ${PHENIX_PATH:-/dev/null}:/opt/phenix:ro

      # Mount built-in skills directory
      - ./skills:/app/skills:ro

      # Mount Docker socket for sibling container execution
      - /var/run/docker.sock:/var/run/docker.sock:ro

    group_add:
      - "${DOCKER_GID:-999}"

    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

    labels:
      - "open_scientist.mode=${OPEN_SCIENTIST_DEV_MODE:-production}"

  # open-scientist-agent: build-only service — never started by compose.
  # Agent containers are spawned at runtime by JobContainerRunner.
  # Build with:  docker compose build open-scientist-agent
  #          or  docker build -f Dockerfile.agent -t open-scientist-agent:latest .
  open-scientist-agent:
    build:
      context: .
      dockerfile: Dockerfile.agent
    image: open-scientist-agent:latest
    profiles: ["build-only"]  # Excluded from `docker compose up`
    entrypoint: ["echo", "open-scientist-agent image built"]

networks:
  default:
    name: open_scientist_default

volumes:
  postgres_data:
