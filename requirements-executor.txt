# Scientific dependencies for OpenScientist executor container
# This is a minimal set - no web framework, no database

# Core scientific computing
pandas>=3.0.0
numpy>=2.2.0,<2.4
scipy>=1.15.0
scikit-learn>=1.6.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0

# Statistics
statsmodels>=0.14.0

# Domain-specific
networkx>=3.0

# Single-cell genomics
scanpy>=1.12.0
anndata>=0.12.0
h5py>=3.10.0

# File format support
openpyxl>=3.1.0
pyarrow>=14.0.0

# HTTP requests (for KBase, external APIs)
requests>=2.31.0

# SPARQL queries against remote endpoints
SPARQLWrapper>=2.0.0

# Bioinformatics
biopython>=1.84

# Dimensionality reduction and clustering
umap-learn>=0.5.0
leidenalg>=0.10.0
python-igraph>=0.11.0

# Better statistical tests (effect sizes, ANOVA, mixed models)
pingouin>=0.5.0

# Fast DataFrames for large datasets
polars>=1.0.0

# Interactive visualization
plotly>=5.0.0

# Differential expression analysis
pydeseq2>=0.4.0

# Simulation — kinetic / systems biology
# Both ship cp312 wheels, so they add no compile time to the image.
libroadrunner>=2.9      # SBML ODE + stochastic solvers, C++ core
copasi-basico>=0.86     # COPASI: reaction DSL, steady state, scans, fitting
#
# Deliberately excluded, do not add without reading this:
#   cobra (FBA) — cobra 0.31.1 requires pandas>=1.0,<3.0, which conflicts with
#     the pandas>=3.0.0 pin above. Leaving it unpinned does NOT fix this: the
#     resolver silently backslides to cobra 0.18.1 (2020), which then fails at
#     import on numpy 2 (`from numpy import float_`). Re-add only once cobra
#     supports pandas 3; tests/test_simulation_stack.py fails when that lands.
#   antimony / tellurium / phrasedml — sdist-only for cp312 linux, so each adds
#     a multi-minute C++ build to the image. basico's reaction DSL covers the
#     same model-authoring ergonomics with a wheel.
# See skills/domain/simulation/SKILL.md.
