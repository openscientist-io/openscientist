notes/
.worktrees/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
.venv/
venv/
ENV/
env/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Environment variables (never commit real secrets)
.env
.env.*
!.env.example
.env.backup-*

# Credential and key material
gcp-credentials.json
codex-auth.json
**/auth.json
service-account*.json
*.pem
*.key
*.p12
*.pfx

# Locally built codex binary for ad-hoc validation. The images build codex
# from the fork in a multi-stage build, so this large artifact never goes in.
codex-local-bin

# Deployment-specific configs (keep private)
docker-compose.*.yml
DEPLOY_*.md

# Logs
logs/*.log

# Job data (keep directory structure, ignore contents)
jobs/*/
jobs_backup*/

# NiceGUI
.nicegui/

# Docker
*.pid

# Phenix installer (large binary, downloaded separately)
Phenix-*.sh
data/phenix-installer-*.tar.gz

# OS
.DS_Store
Thumbs.db

# Test coverage
.coverage
htmlcov/
.pytest_cache/

# uv
.ini
.mypy_cache/
.ruff_cache/
data/

.playwright-mcp/
.playwright-mcp/*.log

# Local archives and backups (use tools/safe_archive.sh for sanitized exports)
*.tar.gz
*.zip
!tests/fixtures/**/*.gz
!tests/fixtures/**/*.zip
