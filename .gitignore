notes/
.worktrees/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
.venv/
venv/
ENV/
env/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Environment variables — broad patterns to catch ad-hoc backups like
# `.env-back`, `.env-back-pre-X`, `env-new-azure`, etc. that have historically
# slipped through narrower rules. These files routinely hold live API keys.
.env
.env.*
.env-*
env-*

# Deployment-specific configs (keep private)
docker-compose.*.yml
DEPLOY_*.md

# Logs
logs/*.log

# Job data (keep directory structure, ignore contents)
jobs/*/
jobs_backup*/

# NiceGUI
.nicegui/

# Docker
*.pid

# Phenix installer (large binary, downloaded separately)
Phenix-*.sh
data/phenix-installer-*.tar.gz

# OS
.DS_Store
Thumbs.db

# Test coverage
.coverage
htmlcov/
.pytest_cache/

# uv
.ini
.mypy_cache/
.ruff_cache/
data/

.playwright-mcp/*.log
shandy-prod-jobs-backup-20260307.tar.gz

# Internal operations notes — deploy plans, outreach drafts, anything that
# may contain user emails, API key holder names, or prod stats. Public docs
# go elsewhere under docs/.
docs/plans/
